# ==============================================================================
# SmartBots — Bank Reconciliation AI Agent
# Python >= 3.11 required
# Install: pip install -r requirements.txt
# ==============================================================================

# ------------------------------------------------------------------------------
# LLM & Agent Orchestration
# ------------------------------------------------------------------------------
openai==1.68.2                  # GPT-4o API + text-embedding-3-small
langgraph==0.3.21               # LangGraph StateGraph agent orchestration
langchain-core==0.3.49          # Base abstractions (Runnables, messages)
langchain-openai==0.3.11        # LangChain ↔ OpenAI integration

# ------------------------------------------------------------------------------
# Vector Store (RAG Layer)
# ------------------------------------------------------------------------------
chromadb==1.0.21                # Local persistent vector store (>=1.0 avoids C build on Windows)

# ------------------------------------------------------------------------------
# API / Web Framework
# ------------------------------------------------------------------------------
fastapi==0.115.11               # Async REST API framework
uvicorn[standard]==0.34.0       # ASGI server for FastAPI
python-multipart==0.0.20        # File upload support for FastAPI
streamlit==1.55.0               # Interactive frontend dashboard

# ------------------------------------------------------------------------------
# Data Parsing & Processing
# ------------------------------------------------------------------------------
pandas==2.2.3                   # CSV parsing & data manipulation
openpyxl==3.1.5                 # Excel (.xlsx) parsing
bai2==0.4.0                     # BAI2 bank statement format parser
lxml==5.3.1                     # XML parsing (ISO 20022 / CAMT.053)

# ------------------------------------------------------------------------------
# Document Ingestion (PDF, Word, Excel)
# ------------------------------------------------------------------------------
PyMuPDF==1.27.2                 # PDF text extraction (fitz)
python-docx==1.2.0              # Word .docx text extraction

# ------------------------------------------------------------------------------
# Fuzzy Matching & NLP
# ------------------------------------------------------------------------------
rapidfuzz==3.12.1               # Fast fuzzy string matching (Levenshtein, token_set_ratio)

# ------------------------------------------------------------------------------
# Data Validation & Schemas
# ------------------------------------------------------------------------------
pydantic==2.11.1                # Strict schema validation (v2)
pydantic-settings==2.8.1        # .env / settings management

# ------------------------------------------------------------------------------
# Metrics & Evaluation
# ------------------------------------------------------------------------------
scikit-learn==1.6.1             # Precision, recall, F1, MCC, confusion matrix
rouge-score==0.1.2              # ROUGE-L for explanation quality evaluation
numpy==2.2.4                    # Numerical operations for metrics

# ------------------------------------------------------------------------------
# Testing
# ------------------------------------------------------------------------------
pytest==8.3.5                   # Test runner
pytest-asyncio==0.25.3          # Async test support (FastAPI, LangGraph)
pytest-cov==6.1.0               # Code coverage reporting
httpx==0.28.1                   # Async HTTP client for FastAPI test client
faker==37.1.0                   # Synthetic test data generation
factory-boy==3.3.3              # Test fixture factories

# ------------------------------------------------------------------------------
# Utilities
# ------------------------------------------------------------------------------
python-dotenv==1.1.0            # Load .env into environment variables
pyyaml==6.0.2                   # Parse config/thresholds.yaml and bank_rules.yaml
tenacity==9.1.2                 # Retry logic for OpenAI API calls
structlog==25.1.0               # Structured JSON logging
rich==14.0.0                    # Pretty CLI output for local dev
